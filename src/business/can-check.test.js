import canCheck from './can-check'

/*
* No tests since this was used for a while in the meteor version
*/
describe('canCheck', () => {
  it('exists', () => {
    expect(canCheck).toBeDefined()
  })
})